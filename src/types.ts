export type Planet = {
  name: string;
  diameter: string;
  climate: string;
  population: string;
};

export type User = {
  name: string;
  height: string;
  mass: number;
  created: Date;
  edited: Date;
  homeworldUrl: string;
};
